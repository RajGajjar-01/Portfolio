---
// Renders one JSON-LD graph node. `set:html` is safe here: the input is always
// a JSON.stringify of our own object, never user content.
interface Props {
	schema: Record<string, unknown>;
}

const { schema } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(schema)} />
